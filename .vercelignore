# Local forScore backups — hundreds of MB, and the app reads them at runtime
# from the user's device, never from the deployment.
*.4sb
*.4ss
test-scores
data

# Local band-hub build — Vercel builds its own; this would just be dead weight.
.next-hub
